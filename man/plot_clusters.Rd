% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network_helpers.R
\name{plot_clusters}
\alias{plot_clusters}
\title{Plot networks with identified communities}
\usage{
plot_clusters(
  net,
  layout = "spring",
  original.categories = NA,
  pointsize = 10,
  concavity = 3,
  legend.title = "",
  labels = NA,
  labelsize = 3,
  radius = 0.04,
  shape = 21,
  outline = F,
  colored = F,
  alpha = 1,
  cluster.base = net,
  fill.colors = NA,
  show.clusters = T,
  ...
)
}
\arguments{
\item{net}{Network produced by \code{\link[bootnet:estimateNetwork]{bootnet::estimateNetwork()}}.}

\item{layout}{Character string or matrix specifying the network layout. Passed to \code{\link[qgraph:qgraph]{qgraph::qgraph()}}. Defaults to \code{"spring"}.}

\item{original.categories}{A \code{data.frame} with two columns, \code{"label"} (categories) and \code{"L1"} (nodes). Used for assigning nodes to theoretical domains.}

\item{pointsize}{Numeric scalar or vector specifying node size(s). Defaults to \code{10}.}

\item{concavity}{Numeric value for the concavity of the cluster hulls. Passed to \code{\link[ggforce:geom_mark_hull]{ggforce::geom_mark_hull()}}. Defaults to \code{3}.}

\item{legend.title}{Character string for the fill legend title. Defaults to \code{""}.}

\item{labels}{Custom node labels, should be in order of the nodes in the \code{net$graph}. Defaults to \code{NA}.}

\item{labelsize}{Numeric value specifying the size of node labels. Defaults to \code{3}.}

\item{radius}{Numeric value specifying the corner radius of cluster hulls. Passed to \code{\link[ggforce:geom_mark_hull]{ggforce::geom_mark_hull()}}. Defaults to \code{0.04}.}

\item{shape}{Numeric value specifying the \code{\link[ggplot2:geom_point]{ggplot2::geom_point()}} shape for nodes. Defaults to \code{21}.}

\item{outline}{Logical. If \code{TRUE}, uses \code{\link[ggplot2:geom_label]{ggplot2::geom_label()}} to draw a box around text. If \code{FALSE}, uses \code{\link[ggplot2:geom_text]{ggplot2::geom_text()}}. Defaults to \code{FALSE}.}

\item{colored}{Logical. If \code{TRUE}, edges are colored (e.g., red/blue for negative/positive). If \code{FALSE}, edge sign is represented by linetype. Defaults to \code{FALSE}.}

\item{alpha}{Numeric value between 0 and 1 specifying edge transparency. Defaults to \code{1}.}

\item{cluster.base}{Network object to use for community detection. Defaults to the \code{net} argument.}

\item{fill.colors}{Vector of character colors for the node fill aesthetic. If \code{NA}, defaults to a colorblind-friendly palette.}

\item{show.clusters}{Logical. If \code{TRUE}, calculates communities and plots convex hulls around them. Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[EGAnet:community.detection]{EGAnet::community.detection()}}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object representing the network graph.
}
\description{
A specialized function to plot network structures, overlaying community detection
(clusters) via convex hulls and allowing for mapping of theoretical domains
to node aesthetics. It utilizes \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} and \code{\link[ggforce:geom_mark_hull]{ggforce::geom_mark_hull()}}
for rendering.
}
\examples{
mtcars = datasets::mtcars
library(bootnet)
library(EGAnet)

# Estimate network
network <- estimateNetwork(mtcars, default = "EBICglasso")

# Plot with clusters
plot_clusters(network, layout = "spring", show.clusters = TRUE)

}
